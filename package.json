{
  "name": "cf-email-fanout-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker for fan-out forwarding to multiple verified destinations",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.13.2"
  }
}
